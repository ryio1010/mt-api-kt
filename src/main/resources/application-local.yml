server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DATASOURCE:jdbc:postgresql://localhost:5433/muscletracking?currentSchema=muscle_tracking}
    username: mtadmin
    password: mtadmin

doma:
  dialect: POSTGRES